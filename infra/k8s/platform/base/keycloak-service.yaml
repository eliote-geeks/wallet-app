apiVersion: v1
kind: Service
metadata:
  name: social-wallet-keycloak
  labels:
    app.kubernetes.io/name: social-wallet-keycloak
spec:
  selector:
    app.kubernetes.io/name: social-wallet-keycloak
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
