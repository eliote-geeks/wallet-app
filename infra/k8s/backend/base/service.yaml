apiVersion: v1
kind: Service
metadata:
  name: social-wallet-backend
  labels:
    app.kubernetes.io/name: social-wallet-backend
spec:
  selector:
    app.kubernetes.io/name: social-wallet-backend
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP

