apiVersion: v1
kind: Service
metadata:
  name: litellm
  namespace: ai-dev
  labels:
    app.kubernetes.io/name: litellm
spec:
  selector:
    app.kubernetes.io/name: litellm
  ports:
    - name: http
      port: 4000
      targetPort: http
      protocol: TCP
