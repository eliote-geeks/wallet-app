apiVersion: v1
kind: Service
metadata:
  name: ai-saas-frontend
  namespace: ai-saas-dev
spec:
  selector:
    app.kubernetes.io/name: ai-saas-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
